import React, { useState } from 'react';
import styled from 'styled-components';

const ScheduleList = () => {
  return <StyledNav>예약 가능 시간</StyledNav>;
};

const StyledNav = styled.nav``;

export default ScheduleList;
